<nav class="bg-gray-900 shadow-md">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <a href="/" class="text-2xl font-bold text-primary">LearnHub</a>
      <div class="hidden md:flex space-x-4">
        <a href="/courses" class="nav-link">Courses</a>
        <% if (user) { %>
        <a href="/dashboard" class="nav-link">Dashboard</a>
        <a href="/user/profile" class="nav-link">Profile</a>
        <% if (user.role === 'student'){ %>
        <a href="/cart" class="nav-link">Cart</a>
        <% } else { %> <% } %>
        <a href="/logout" class="nav-link">Logout</a>

        <% } else { %>
        <a href="/login" class="nav-link">Login</a>
        <a href="/register" class="nav-link">Register</a>
        <% } %>
      </div>
      <div class="md:hidden">
        <button
          id="mobile-menu-button"
          class="text-gray-500 hover:text-gray-600"
        >
          <i class="fas fa-bars"></i>
        </button>
      </div>
    </div>
  </div>
  <div id="mobile-menu" class="hidden md:hidden">
    <a href="/courses" class="block py-2 px-4 text-sm hover:bg-gray-600"
      >Courses</a
    >
    <% if (user) { %>
    <a href="/dashboard" class="block py-2 px-4 text-sm hover:bg-gray-600"
      >Dashboard</a
    >
    <a href="/user/profile" class="block py-2 px-4 text-sm hover:bg-gray-600"
      >Profile</a
    >
    <% if (user.role === 'student'){ %>
    <a href="/cart" class="block py-2 px-4 text-sm hover:bg-gray-600">Cart</a>
    <% } else { %> <% } %>
    <a href="/logout" class="block py-2 px-4 text-sm hover:bg-gray-600"
      >Logout</a
    >
    <% } else { %>
    <a href="/login" class="block py-2 px-4 text-sm hover:bg-gray-100">Login</a>
    <a href="/register" class="block py-2 px-4 text-sm hover:bg-gray-100"
      >Register</a
    >
    <% } %>
  </div>
</nav>
